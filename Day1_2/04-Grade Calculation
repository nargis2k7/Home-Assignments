 function studentscore(score) {
    let grade;
    switch (true) {
        case score > 90:
            grade = 'A'
            break;
        case score > 80:
            grade = 'B'
            break;
        case score > 85:
            grade = 'C'
            break;
        case score > 70:
            grade = 'D'
            break;
        case score > 60:
            grade = 'E'
            break;
            defaul:
            grade = 'F'
                }
                return grade;
}

const studentscr = 75;
const studentgrade = studentscore(studentscr);

console.log(`Garde is ${studentgrade}`); 

